#include "main.h"

/**
 * main - prints the name of program.
 * @argc: the number of argument passed
 * @argv: the vector that hold the argument passed.
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
