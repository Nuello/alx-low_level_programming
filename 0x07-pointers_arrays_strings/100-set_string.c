#include "main.h"

/**
 * set_string - sets the value of a pointer to a char
 * @s: a pointer to pointer holding the final copy
 * @to: a pointer to the address to be copied
 * Return: Always void
 */

void set_string(char **s, char *to)
{
	s = &to;
	**s = 
}
