#include "main.h"

/**
 * mul - multiplies two integer
 * @a: integer to be multiplied
 * @b: the 2nd integer to be multiplied by the 1st
 * Return: 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
