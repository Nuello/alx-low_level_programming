#include "main.h"

/**
 * _abs - computes the absol value of an int
 *@n: The number to find the absolute value.
 * Return: abs of n
 */

int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
